# Environment Variables - Confide Frontend

# API Base URL (set in Vercel when backend is deployed)
# Leave empty or omit to use offline mode only
VITE_API_BASE_URL=

# Example for production:
# VITE_API_BASE_URL=https://api.confide.app/confide/api/exam
